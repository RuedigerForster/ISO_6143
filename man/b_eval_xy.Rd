% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/b_eval.R
\name{b_eval_xy}
\alias{b_eval_xy}
\title{Evaluate fitted function and return full x-y covariance structure}
\usage{
b_eval_xy(meas_data, b, b_cov = NULL, func = NULL)
}
\arguments{
\item{meas_data}{numeric matrix with \eqn{m} rows and two columns:
instrument response \eqn{y}, standard uncertainty \eqn{u(y)}.}

\item{b}{a \code{B_Least} object \emph{or} a numeric vector of
fitted coefficients.}

\item{b_cov}{covariance matrix of \code{b} (required when \code{b} is a
numeric vector).}

\item{func}{calibration function (required when \code{b} is a numeric
vector).}
}
\value{
A list with elements:
  \describe{
    \item{x}{numeric vector of assigned values.}
    \item{y}{numeric vector of instrument responses (same as
      \code{meas_data[, 1]}).}
    \item{x_cov}{covariance matrix of \code{x} (\eqn{m \times m}).}
    \item{y_cov}{covariance matrix of \code{y} (\eqn{m \times m}).}
    \item{xy_cov}{cross-covariance matrix of \code{x} and \code{y}
      (\eqn{m \times m}).}
  }
}
\description{
Like \code{\link{b_eval}} but also returns the covariance of the
(adjusted) instrument responses and the cross-covariance between
assigned values and responses.
}
